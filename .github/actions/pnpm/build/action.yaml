name: pnpm.build
description: Allows build svelte project

inputs:
  working-directory:
    description: Working directory
    required: true

runs:
  using: composite
  steps:
    - name: Build
      run: pnpm build
      shell: bash
      working-directory: ${{ inputs.working-directory }}